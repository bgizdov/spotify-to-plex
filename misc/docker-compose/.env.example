# Minimal yt-dlp-host Configuration
# No resources, no network, just ports and volumes

# API Key (generate: openssl rand -hex 32)
API_KEY=your-api-key-here

# Optional: Change port if 5000 is taken
# YTDLP_API_PORT=5000

# Optional: Change download path
# DOWNLOAD_PATH=/mnt/plex/Music
